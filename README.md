# QR_Indoor_Navigation
The indoor navigation system uses QR code markers at junctions to identify a user's current location within a building. By scanning these markers, the system determines the corresponding graph node and calculates the shortest path to a selected destination. 
